# Zabbix agent UserParameter config for Windows DHCP server
#
# Deploy to: C:\Program Files\Zabbix Agent 2\zabbix_agent2.d\dhcp.conf
#   (or zabbix_agentd.d\ for agent 1)
#
# After adding this file, restart the Zabbix Agent service:
#   Restart-Service "Zabbix Agent 2"
#
# Add the item on the DHCP server host in Zabbix:
#   Key:      dhcp.leases
#   Type:     Zabbix agent (active or passive)
#   Type of information: Text
#   Update interval: 5m
#   History storage period: 1h (lease table changes constantly)
#   Trends: Do not keep
#   Timeout: 30s     <-- IMPORTANT: set at the ITEM level, not the agent level
#
# Note on timeouts: Zabbix agent's global "Timeout" setting belongs in the main
# zabbix_agent2.conf (valid range 1-30 seconds) and cannot be overridden from
# an include-dir file. Setting it here produces:
#   "wrong value of 'Timeout' in config file"
#
# If dhcp-leases.ps1 runs slowly on a busy DHCP server (many thousand leases),
# increase the PER-ITEM timeout in the Zabbix item definition (7.0+ supports
# 1s-600s per item). You typically do NOT need to touch the global agent
# Timeout - the item-level setting is the one that matters for agent checks.

# UserParameter: dhcp.leases invokes the PS script and returns its JSON output.
#   -NoProfile         skips user profile load (~500ms faster)
#   -ExecutionPolicy Bypass  lets unsigned scripts run
#   -File              ensures the script is executed, not interpreted as a command
UserParameter=dhcp.leases,powershell.exe -NoProfile -ExecutionPolicy Bypass -File "C:\zabbix\scripts\dhcp-leases.ps1"
